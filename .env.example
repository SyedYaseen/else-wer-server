# Local native dev (`make dev` / `cargo run`; dotenv loads ./.env).
# `make env-init` copies this to .env and fills JWT_SECRET. See src/config.rs for defaults.

JWT_SECRET=
AUDIOBOOKS_LOCATION=/home/loop/p/else-wer/else-wer-server/data/JRRTolkien
DATABASE_URL=sqlite:./else-wer.db
HOST="0.0.0.0"
PORT=3000
JWT_LOC="../creds/jwt.key"
# false = relay mode (was .env.local)
SELF_HOSTED=true
RELAY_URI=http://localhost:9000/api
RUST_LOG="info"
